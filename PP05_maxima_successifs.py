# Pour la lecture
import numpy as np
# Lecture des donnees
donnees_t,donnees_u = np.loadtxt('Uc_41.txt',unpack=True)

# Trouvez le moyen de determiner les valeurs de t (et de u) correspondant aux 
# maxima successifs
def temps_et_maxima(t,u):
    liste_t,liste_u = [],[]
    return liste_t,liste_u
    
    
# Reste a afficher le resultat apres application de la fonction sur les donnees
troisieme_temps_maxima = 'Attention, le troisieme est en position...'

print(troisieme_temps_maxima)
